# Rename this file to `.env.local` and fill in your own keys.

# Groq LLM API key used for generating summaries and trade plans.
# Leave this blank to use built‑in summary and trade plan templates (no external API calls).
GROQ_API_KEY=

# Stripe secret key used for creating checkout sessions. Optional in test mode.
STRIPE_SECRET_KEY=

# Price ID for your Stripe subscription (must be a recurring product). Optional in test mode.
STRIPE_PRICE_ID=

# Stripe publishable key for client‑side Stripe.js integration. Leave blank to auto‑unlock the Pro plan for testing.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=